# Bu sunucunun LAN IP'si (ip -4 addr | grep 192.168)
# Aynı PC test: 127.0.0.1 | Başka cihaz: gerçek IP (ör. 192.168.0.90)
MTX_PUBLIC_HOST=192.168.0.90

# Upload limiti (bayt). Varsayılan ~50 GB. Sınırsız için: 0
MAX_UPLOAD_BYTES=53687091200
